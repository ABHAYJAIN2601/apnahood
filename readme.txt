to run and build this code

follow this command

npm i (for installing package)

npm run server (to run with nodemon)

npm run start (to run without nodemon)


if above things works 
you will see console of 
server connected 
mongodb connected